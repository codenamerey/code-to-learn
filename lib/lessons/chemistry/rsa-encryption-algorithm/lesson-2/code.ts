export const defaultCode = `function generateRSAKeys(p, q) {
  // Step 1: Validate that p and q are prime
  
  
  // Step 2: Calculate n and φ(n)
  
  
  // Step 3: Find a valid public exponent e
  // Start from e=3 and find the first value where GCD(e, phi) = 1
  
  
  // Step 4: Create RSAKey and set the public exponent
  
  
  // Step 5: Return the key
  
  
}`;
