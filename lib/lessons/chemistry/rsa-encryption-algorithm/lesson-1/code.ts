export const defaultCode = `function generateRSAKeys(p, q) {
  // Step 1: Check if p is prime
  
  
  // Step 2: Check if q is prime
  
  
  // Step 3: Calculate n = p * q
  
  
  // Step 4: Calculate φ(n) = (p-1) * (q-1)
  
  
  // Step 5: Create and return an RSAKey object
  
  
}`;
