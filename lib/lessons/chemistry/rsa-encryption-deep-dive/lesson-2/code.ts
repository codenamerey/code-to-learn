export const defaultCode = `/**
 * Calculates (base^exp) % mod using modular exponentiation (exponentiation by squaring).
 * All inputs and outputs are BigInts.
 * @param {BigInt} base The base number.
 * @param {BigInt} exp The exponent.
 * @param {BigInt} mod The modulus.
 * @returns {BigInt} The result of (base^exp) % mod.
 */
function convertMessageToNumber(base, exp, mod) {
  // 1. Initialize result to 1n.

  // 2. Reduce base modulo mod.

  // 3. Loop while exp is greater than 0n.

    // 3a. If exp is odd, update result.

    // 3b. Square the base and reduce modulo mod.

    // 3c. Halve exp.

  // 4. Return result.
  return 0n;
}`;
