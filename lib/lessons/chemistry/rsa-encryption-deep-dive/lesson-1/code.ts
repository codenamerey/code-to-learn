export const defaultCode = `/**
 * Converts a message string into a numerical representation.
 * Each character's ASCII value is concatenated to form a large number string,
 * which is then converted to a BigInt.
 * @param {string} message The message to convert.
 * @returns {BigInt} The numerical representation of the message.
 */
function convertMessageToNumber(message) {
  // 1. Initialize an empty string to store concatenated ASCII values.

  // 2. Iterate through each character of the message.

  // 3. For each character, get its ASCII (code point) value.

  // 4. Convert the ASCII value to a string and append it to the concatenated string.

  // 5. Convert the final concatenated string to a BigInt.
  //    If the concatenated string is empty, return 0n.

  // 6. Return the BigInt representation.
  return 0n;
}`;
